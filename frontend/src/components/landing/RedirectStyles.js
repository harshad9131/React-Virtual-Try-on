// This file contains inline styles for the redirect page that were embedded in redirect.html
// These styles are already included in style.css, but if needed for landing page specific styles,
// they would be defined here or in a separate CSS module.

// For now, all styles are in the main style.css file

